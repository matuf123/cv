# CV HTML/CSS/JS — Paket (Harga: Rp 1.000.000.000.000)

Deskripsi singkat:
- Paket ini berisi: `index.html`, `styles.css`, `script.js`.
- Tampilan modern, responsif, dan siap dicetak ke PDF.
- Terdapat label harga eksplisit: Rp 1.000.000.000.000 (satu triliun rupiah).

Cara pakai:
1. Letakkan ketiga file pada satu folder.
2. Buka `index.html` di browser untuk melihat.
3. Edit isi (nama, pengalaman, kontak) langsung di `index.html`.
4. Gunakan tombol "Cetak" atau `Ctrl+P` untuk menyimpan sebagai PDF.

Kustomisasi cepat:
- Ubah warna di `:root` pada `styles.css`.
- Ganti teks harga di elemen dengan id `bigPrice` di `index.html`.
- Untuk menambahkan foto asli, ganti bagian SVG dengan tag `<img src="foto.jpg" alt="Nama Anda">`.

Lisensi:
- Bebas digunakan dan dimodifikasi untuk kebutuhan Anda.
